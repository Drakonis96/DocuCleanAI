services:
  docuclean-ai:
    build: .
    ports:
      - "5037:5037"
    volumes:
      - ./data:/app/data
      - ./.env.local:/app/.env.local
    environment:
      - NODE_ENV=production
    restart: unless-stopped
